language: java
sudo: false # faster builds

script: "mvn clean compile"

deploy:
  provider: pages
  skip-cleanup: true
  local_dir: target
  on:
    branch: master
cache:
  directories:
  - "$HOME/.cache"
branches:
  except:
  - legacy
  - experimental

# safelist
branches:
  only:
  - master
  - stable