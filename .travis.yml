language: java
sudo: false # faster builds

script: "mvn clean install"

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: target
  on:
    branch: master
cache:
  directories:
  - "$HOME/.cache"